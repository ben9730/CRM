---
phase: 05-portal-foundation-api-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/chat/tools.ts
  - src/app/api/chat/route.ts
  - src/app/layout.tsx
autonomous: true
requirements:
  - AITOOL-05
  - PORTAL-04

must_haves:
  truths:
    - "Tool definitions exist in a shared module importable by both the chat widget and the portal"
    - "When Gemini rate limit is exceeded, API returns a structured JSON response with rateLimited flag and friendly message"
    - "The existing ChatWidget continues to work identically after the refactor"
  artifacts:
    - path: "src/lib/chat/tools.ts"
      provides: "Shared tool definitions and executeTool function"
      exports: ["chatTools", "executeTool", "SYSTEM_PROMPT"]
    - path: "src/app/api/chat/route.ts"
      provides: "Chat API with rate limit handling and tool imports from shared module"
      contains: "import { chatTools, executeTool"
    - path: "src/app/layout.tsx"
      provides: "Root layout with viewport-fit=cover for safe area insets"
      contains: "viewportFit"
  key_links:
    - from: "src/app/api/chat/route.ts"
      to: "src/lib/chat/tools.ts"
      via: "import"
      pattern: "import.*chatTools.*executeTool.*from.*@/lib/chat/tools"
    - from: "src/app/api/chat/route.ts"
      to: "NextResponse.json"
      via: "rate limit catch block"
      pattern: "rateLimited.*true.*friendlyMessage"
---

<objective>
Extract chat tool definitions to a shared module and add rate limit protection to the API route.

Purpose: AITOOL-05 requires tool definitions in a shared module so Phase 6 can add new tools cleanly. PORTAL-04 requires the API to return structured rate limit responses so the portal (Plan 02) can render a friendly error bubble instead of raw error text. Adding viewport-fit=cover to the root layout enables safe area insets for the portal's mobile layout.

Output: src/lib/chat/tools.ts (new shared module), refactored route.ts with rate limit handling, root layout.tsx with viewport export
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-portal-foundation-api-safety/05-RESEARCH.md

@src/app/api/chat/route.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract tool definitions and executeTool to shared module</name>
  <files>src/lib/chat/tools.ts, src/app/api/chat/route.ts</files>
  <action>
Create src/lib/chat/tools.ts as a new shared module:
1. Create the directory src/lib/chat/ if it does not exist
2. Move the SYSTEM_PROMPT constant from route.ts to tools.ts, exported as `export const SYSTEM_PROMPT`
3. Move the entire `tools` array (FunctionDeclaration[]) from route.ts to tools.ts, exported as `export const chatTools`
4. Move the entire `executeTool` function from route.ts to tools.ts, exported as `export async function executeTool`
5. Move all imports that these depend on into tools.ts: `FunctionDeclaration`, `FunctionResponsePart`, `SchemaType` from `@google/generative-ai`, `getAccountId` from `@/lib/queries/account`, `getLocalToday`, `toDateOnly` from `@/lib/utils`

Then update src/app/api/chat/route.ts:
1. Remove the moved code (SYSTEM_PROMPT, tools array, executeTool function, and their dedicated imports)
2. Add: `import { chatTools, executeTool, SYSTEM_PROMPT } from '@/lib/chat/tools'`
3. Update the model configuration to use `chatTools` instead of `tools`: `tools: [{ functionDeclarations: chatTools }]`
4. Add `export const maxDuration = 30` at the top of route.ts (before the POST function) per STATE.md decision -- prevents Vercel 10s timeout for multi-tool-call queries
5. Keep all other route.ts code (POST handler, auth check, chat loop, error handling) exactly as-is for now

This is a pure refactor -- the API behavior must be identical before and after. The ChatWidget (which calls /api/chat) must continue to work without any changes.
  </action>
  <verify>
    <automated>cd "D:/claude code/CRM" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Run the dev server and test the existing ChatWidget still works: open CRM, click the chat bubble, send "show urgent tasks" -- should get a response</manual>
  </verify>
  <done>src/lib/chat/tools.ts exports chatTools, executeTool, and SYSTEM_PROMPT. route.ts imports from the shared module. TypeScript compiles without errors. Existing ChatWidget works identically.</done>
</task>

<task type="auto">
  <name>Task 2: Add rate limit detection and viewport-fit=cover</name>
  <files>src/app/api/chat/route.ts, src/app/layout.tsx</files>
  <action>
Update the catch block in src/app/api/chat/route.ts POST handler:
1. After the existing `console.error('Chat API error:', err)` and `const errorMessage = ...` lines
2. Add rate limit detection: check `errorMessage.includes('429') || errorMessage.includes('RESOURCE_EXHAUSTED')`
3. If rate limited, return: `NextResponse.json({ rateLimited: true, friendlyMessage: "I'm taking a breather -- try again in a minute" }, { status: 429 })`
4. Keep the existing generic error return for non-rate-limit errors unchanged

Update src/app/layout.tsx:
1. Add `import type { Viewport } from 'next'` (alongside existing Metadata import)
2. Add a new export: `export const viewport: Viewport = { width: 'device-width', initialScale: 1, viewportFit: 'cover' }`
3. This enables env(safe-area-inset-bottom) to work on iPhone for the portal's mobile layout
4. Do NOT change anything else in layout.tsx -- the existing metadata export, RootLayout function, ThemeProvider, fonts all stay identical
  </action>
  <verify>
    <automated>cd "D:/claude code/CRM" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check that the dev server starts without errors. The viewport-fit=cover can be verified by inspecting the HTML source for the viewport meta tag.</manual>
  </verify>
  <done>API route returns structured { rateLimited: true, friendlyMessage } on 429/RESOURCE_EXHAUSTED errors. Root layout exports viewport with viewportFit: 'cover'. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully (no build errors from the refactor)
3. src/lib/chat/tools.ts exists and exports chatTools (array), executeTool (function), SYSTEM_PROMPT (string)
4. src/app/api/chat/route.ts imports from '@/lib/chat/tools' and has maxDuration = 30
5. The catch block in route.ts handles rate limit errors with a structured response
6. src/app/layout.tsx has a viewport export with viewportFit: 'cover'
</verification>

<success_criteria>
- Tool definitions live in src/lib/chat/tools.ts, not in route.ts
- Rate limit errors return { rateLimited: true, friendlyMessage: "..." } with status 429
- maxDuration = 30 exported from route.ts
- viewport-fit=cover in root layout
- Existing ChatWidget works unchanged (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/05-portal-foundation-api-safety/05-01-SUMMARY.md`
</output>
