---
phase: 02-backend-data-layer
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/proxy.ts]
autonomous: true
requirements: [AUTH-04]
gap_closure: true

must_haves:
  truths:
    - "Unauthenticated users visiting / are redirected to /login"
    - "Authenticated users are not affected by the proxy move"
  artifacts:
    - path: "src/proxy.ts"
      provides: "Auth guard proxy at correct location for src/ directory structure"
      contains: "export async function proxy"
  key_links:
    - from: "src/proxy.ts"
      to: "src/lib/supabase/proxy.ts"
      via: "import updateSession"
      pattern: "import.*updateSession.*@/lib/supabase/proxy"
---

<objective>
Fix auth guard redirect by moving proxy.ts from project root to src/proxy.ts.

Purpose: The proxy never runs because Next.js 16 requires proxy.ts at the same level as the app/ directory. Since the project uses src/app/, the proxy must be at src/proxy.ts. The middleware manifest is currently empty, meaning unauthenticated users bypass auth guards entirely.

Output: Working auth guard that redirects unauthenticated users to /login.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/auth-guard-not-redirecting.md
@.planning/phases/02-backend-data-layer/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move proxy.ts to src/proxy.ts and verify build registration</name>
  <files>src/proxy.ts</files>
  <action>
    Move proxy.ts from the project root to src/proxy.ts. The file content stays EXACTLY the same — no edits to the code, imports, or config. The @/lib/supabase/proxy import resolves identically after the move because @ maps to src/.

    Steps:
    1. Copy proxy.ts content to src/proxy.ts (use git mv or move + delete)
    2. Delete the original proxy.ts at project root
    3. Run `npx next build` and inspect .next/server/middleware-manifest.json
    4. Confirm the middleware manifest is NO LONGER empty — it should now contain a proxy/middleware entry with the matcher pattern

    Do NOT modify src/lib/supabase/proxy.ts — that file is correct and unchanged.
    Do NOT modify any other files — this is a file location fix only.
  </action>
  <verify>
    Run `npx next build` and then check:
    1. `proxy.ts` does NOT exist at project root
    2. `src/proxy.ts` EXISTS with the proxy function export
    3. `.next/server/middleware-manifest.json` contains a non-empty middleware or functions entry (not empty `{}`)
    4. Build completes without errors
  </verify>
  <done>
    proxy.ts is at src/proxy.ts, the old file at project root is deleted, and `next build` registers the proxy in the middleware manifest (non-empty sortedMiddleware array).
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel and verify auth redirect</name>
  <files></files>
  <action>
    Deploy the fix to Vercel so the production site enforces auth guards.

    Steps:
    1. Commit the proxy.ts move
    2. Run `npx vercel --yes --prod` to deploy (or push to trigger auto-deploy if configured)
    3. After deployment completes, use curl to verify the redirect:
       - `curl -I https://healthcrm-tawny.vercel.app/` should return a 307 redirect to /login (when no auth cookie is present)
       - `curl -I https://healthcrm-tawny.vercel.app/dashboard` should also redirect to /login

    The deployment URL is https://healthcrm-tawny.vercel.app per the existing debug session.
  </action>
  <verify>
    1. `curl -I https://healthcrm-tawny.vercel.app/` returns 307 with Location: /login (or similar redirect status)
    2. `curl -I https://healthcrm-tawny.vercel.app/dashboard` returns 307 with Location: /login
    3. No build errors in Vercel deployment output
  </verify>
  <done>
    The production deployment at healthcrm-tawny.vercel.app redirects unauthenticated users to /login when visiting / or /dashboard. UAT test 1 now passes.
  </done>
</task>

</tasks>

<verification>
- proxy.ts no longer exists at project root
- src/proxy.ts contains the proxy function with updateSession import
- Local build shows proxy registered in middleware manifest
- Production deployment redirects unauthenticated users to /login
- Authenticated users can still access /dashboard normally (no regression)
</verification>

<success_criteria>
Unauthenticated visitors to https://healthcrm-tawny.vercel.app/ are redirected to /login. The UAT gap "Unauthenticated users visiting / are redirected to /login" is closed. All 8/8 UAT tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-data-layer/02-04-SUMMARY.md`
</output>
