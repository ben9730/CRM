---
phase: 02-backend-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proxy.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/proxy.ts
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/(auth)/forgot-password/page.tsx
  - src/app/(auth)/auth/callback/route.ts
  - src/app/(app)/layout.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/contacts/page.tsx
  - src/app/(app)/contacts/[id]/page.tsx
  - src/app/(app)/deals/page.tsx
  - src/app/(app)/organizations/page.tsx
  - src/app/(app)/tasks/page.tsx
  - src/app/(app)/interactions/page.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/types/database.ts
  - .env.local
  - .gitignore
autonomous: false
requirements:
  - ARCH-04
  - ARCH-05

user_setup:
  - service: vercel
    why: "Deployment platform for Next.js"
    env_vars: []
    dashboard_config:
      - task: "Connect Vercel to GitHub repo if not already connected"
        location: "Vercel Dashboard -> Import Project"

must_haves:
  truths:
    - "Supabase project exists and is accessible via MCP tools"
    - "Next.js app builds without errors with Supabase client libraries installed"
    - "proxy.ts at project root refreshes Supabase auth session on every request"
    - "Route groups separate auth pages from app pages with distinct layouts"
    - "Application is deployed to Vercel and accessible from a live URL"
    - "Environment variables are configured in both .env.local and Vercel"
  artifacts:
    - path: "proxy.ts"
      provides: "Session refresh proxy (Next.js 16 convention)"
      contains: "export async function proxy"
    - path: "src/lib/supabase/client.ts"
      provides: "Browser Supabase client for Client Components"
      contains: "createBrowserClient"
    - path: "src/lib/supabase/server.ts"
      provides: "Server Supabase client for Server Components and Actions"
      contains: "createServerClient"
    - path: "src/lib/supabase/proxy.ts"
      provides: "updateSession function used by proxy.ts"
      contains: "updateSession"
    - path: "src/app/(auth)/layout.tsx"
      provides: "Centered card layout for auth pages"
    - path: "src/app/(app)/layout.tsx"
      provides: "App shell layout with sidebar and header"
    - path: "src/types/database.ts"
      provides: "TypeScript types for Supabase schema"
  key_links:
    - from: "proxy.ts"
      to: "src/lib/supabase/proxy.ts"
      via: "import updateSession"
      pattern: "import.*updateSession.*from.*lib/supabase/proxy"
    - from: "src/lib/supabase/server.ts"
      to: "Supabase"
      via: "NEXT_PUBLIC_SUPABASE_URL env var"
      pattern: "process\\.env\\.NEXT_PUBLIC_SUPABASE_URL"
    - from: "src/lib/supabase/client.ts"
      to: "Supabase"
      via: "NEXT_PUBLIC_SUPABASE_URL env var"
      pattern: "process\\.env\\.NEXT_PUBLIC_SUPABASE_URL"
---

<objective>
Create the Supabase project, restructure the Next.js app with route groups and Supabase client libraries, set up proxy.ts for session refresh, and deploy to Vercel.

Purpose: Establish the infrastructure foundation that every subsequent plan depends on. Without a live Supabase project, no schema or auth work can happen. Without route groups, auth and app layouts cannot be separated. Without Vercel deployment, there is no live URL for testing.

Output: A live Supabase project, a restructured Next.js app with Supabase clients wired, proxy.ts handling session refresh, and a Vercel deployment accessible via URL.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-data-layer/02-RESEARCH.md
@.planning/phases/01-frontend-design-ui/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase project and install client libraries</name>
  <files>
    package.json
    .env.local
    .gitignore
    src/lib/supabase/client.ts
    src/lib/supabase/server.ts
    src/lib/supabase/proxy.ts
    src/types/database.ts
  </files>
  <action>
1. Create a new Supabase project via MCP tool `create_project`:
   - Name: "HealthCRM"
   - Region: us-east-1
   - Organization: ask user which org to use (use `list_organizations` first)
   - Follow the cost confirmation flow (`get_cost` -> `confirm_cost` -> `create_project`)

2. Wait for project to be ready (use `get_project` to check status until ACTIVE_HEALTHY).

3. Get project URL and publishable key:
   - Use `get_project_url` for the API URL
   - Use `get_publishable_keys` for the publishable key

4. Install Supabase client libraries:
   ```bash
   npm install @supabase/supabase-js @supabase/ssr
   ```

5. Install form validation libraries (needed for auth forms in Plan 02-03):
   ```bash
   npm install zod react-hook-form @hookform/resolvers
   ```

6. Create `.env.local` with Supabase credentials:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://[project-ref].supabase.co
   NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=[publishable-key]
   NEXT_PUBLIC_SITE_URL=http://localhost:3000
   ```
   NOTE: Do NOT include the service_role key in .env.local. It is not needed for Phase 2 client-side operations.

7. Ensure `.gitignore` includes `.env.local` and `.env*.local`.

8. Create `src/lib/supabase/client.ts` — browser client using `createBrowserClient` from `@supabase/ssr`. Uses `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`. Follow the exact pattern from 02-RESEARCH.md Pattern 3.

9. Create `src/lib/supabase/server.ts` — server client using `createServerClient` from `@supabase/ssr`. Uses `await cookies()` from `next/headers`. The `setAll` method wraps in try/catch because Server Components cannot set cookies. Follow the exact pattern from 02-RESEARCH.md Pattern 2.

10. Create `src/lib/supabase/proxy.ts` — the `updateSession` function. Creates a Supabase client with cookie handlers that write to both `request.cookies` and `response.cookies`. Calls `await supabase.auth.getUser()` (NOT `getSession()`) to validate and refresh the session. Include auth guard logic: redirect unauthenticated users from protected routes to `/login`, redirect authenticated users from auth routes (`/login`, `/signup`, `/forgot-password`) to `/dashboard`. Follow 02-RESEARCH.md Pattern 1 + Pattern 4 combined.

11. Create `src/types/database.ts` as a placeholder with a `Database` type that will be regenerated after schema is applied in Plan 02-02. For now, export a minimal type:
    ```typescript
    export type Database = Record<string, never>
    ```

12. Disable email confirmation in Supabase dashboard. This cannot be done via MCP — note it for the checkpoint. The setting is: Authentication > Providers > Email > Confirm Email: OFF.
  </action>
  <verify>
    - `npm ls @supabase/supabase-js @supabase/ssr` shows both installed
    - `npm ls zod react-hook-form @hookform/resolvers` shows all installed
    - `.env.local` exists with NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
    - `src/lib/supabase/client.ts`, `server.ts`, and `proxy.ts` all exist
    - `src/types/database.ts` exists
    - Supabase project status is ACTIVE_HEALTHY via `get_project`
  </verify>
  <done>Supabase project is live, client libraries installed, three Supabase client utilities created (browser, server, proxy), environment variables configured</done>
</task>

<task type="auto">
  <name>Task 2: Restructure app with route groups, proxy.ts, and deploy to Vercel</name>
  <files>
    proxy.ts
    src/app/layout.tsx
    src/app/page.tsx
    src/app/(auth)/layout.tsx
    src/app/(auth)/login/page.tsx
    src/app/(auth)/signup/page.tsx
    src/app/(auth)/forgot-password/page.tsx
    src/app/(auth)/auth/callback/route.ts
    src/app/(app)/layout.tsx
    src/app/(app)/dashboard/page.tsx
    src/app/(app)/contacts/page.tsx
    src/app/(app)/contacts/[id]/page.tsx
    src/app/(app)/deals/page.tsx
    src/app/(app)/organizations/page.tsx
    src/app/(app)/tasks/page.tsx
    src/app/(app)/interactions/page.tsx
  </files>
  <action>
1. Create `proxy.ts` at the PROJECT ROOT (NOT in src/). This is a Next.js 16 file convention — it MUST be named `proxy.ts` (not `middleware.ts`). Export `proxy` function (not `middleware`). Import `updateSession` from `@/lib/supabase/proxy`. Include the matcher config that excludes static files. Follow 02-RESEARCH.md Pattern 1 exactly.

   CRITICAL: The file name is `proxy.ts`, the function name is `proxy`. Getting this wrong silently fails — no errors, just broken sessions.

2. Restructure the app directory into route groups:
   - Create `src/app/(auth)/` route group for auth pages (unauthenticated layout)
   - Create `src/app/(app)/` route group for the main app (authenticated layout with sidebar)
   - Move existing pages into `(app)`:
     - `src/app/page.tsx` content (dashboard) -> `src/app/(app)/dashboard/page.tsx`
     - `src/app/contacts/` -> `src/app/(app)/contacts/`
     - `src/app/deals/` -> `src/app/(app)/deals/`
     - `src/app/organizations/` -> `src/app/(app)/organizations/`
     - `src/app/tasks/` -> `src/app/(app)/tasks/`
     - `src/app/interactions/` -> `src/app/(app)/interactions/`
   - Delete old page files after moving

3. Update `src/app/layout.tsx` (root layout):
   - Keep the HTML, body, fonts, ThemeProvider
   - Remove the AppShell (sidebar + header) — that moves to `(app)/layout.tsx`
   - Root layout is now a clean wrapper with just fonts, theme, and {children}

4. Create `src/app/page.tsx` as a redirect to `/dashboard`:
   ```typescript
   import { redirect } from 'next/navigation'
   export default function Home() { redirect('/dashboard') }
   ```

5. Create `src/app/(app)/layout.tsx`:
   - Move the AppShell layout (SidebarProvider, AppSidebar, AppHeader) from the old root layout into here
   - This layout wraps all authenticated pages
   - Import existing sidebar and header components

6. Create `src/app/(auth)/layout.tsx`:
   - Minimal centered layout for auth forms
   - Dark background matching the app theme (oklch(0.10))
   - Center children vertically and horizontally
   - Max width container (e.g., max-w-md) for form cards
   - Include the app logo/name at the top

7. Create placeholder auth pages (functional UI will be built in Plan 02-03, but pages must exist for routing):
   - `src/app/(auth)/login/page.tsx` — placeholder with "Login" heading and link to signup
   - `src/app/(auth)/signup/page.tsx` — placeholder with "Sign Up" heading and link to login
   - `src/app/(auth)/forgot-password/page.tsx` — placeholder with "Reset Password" heading
   - `src/app/(auth)/auth/callback/route.ts` — PKCE callback Route Handler. Exchanges `code` query param for session via `supabase.auth.exchangeCodeForSession(code)`. Redirects to `next` query param or `/dashboard`. On error, redirects to `/login?error=auth_callback_error`. Follow 02-RESEARCH.md Pattern 10.

8. Verify the build passes:
   ```bash
   npm run build
   ```
   Fix any TypeScript or import errors.

9. Deploy to Vercel:
   - Run `npx vercel --yes` to deploy (this will create the Vercel project if it does not exist)
   - If auth error, create a checkpoint for the user to authenticate via `npx vercel login`
   - After deployment, set environment variables on Vercel:
     ```bash
     npx vercel env add NEXT_PUBLIC_SUPABASE_URL production < value
     npx vercel env add NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY production < value
     npx vercel env add NEXT_PUBLIC_SITE_URL production < value
     ```
     Set NEXT_PUBLIC_SITE_URL to the Vercel deployment URL.
   - Redeploy after setting env vars: `npx vercel --prod`

10. Update `NEXT_PUBLIC_SITE_URL` in `.env.local` to match the Vercel URL for password reset redirect URLs.

11. Configure Supabase Auth URL settings: the Site URL and Redirect URLs should include the Vercel domain. This may need to be done in the Supabase dashboard — note it for the checkpoint.
  </action>
  <verify>
    - `proxy.ts` exists at project root with `export async function proxy`
    - `src/app/(auth)/` directory exists with login, signup, forgot-password pages and auth/callback route
    - `src/app/(app)/` directory exists with dashboard, contacts, deals, organizations, tasks, interactions pages
    - `src/app/(app)/layout.tsx` contains AppShell (sidebar + header)
    - `src/app/(auth)/layout.tsx` contains centered card layout
    - `npm run build` succeeds
    - Vercel deployment URL is accessible in browser
  </verify>
  <done>Next.js app restructured with (auth) and (app) route groups, proxy.ts handles session refresh, all Phase 1 pages moved into (app) group, auth page placeholders created, app deployed to Vercel and accessible via live URL</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify infrastructure and complete manual Supabase dashboard config</name>
  <action>
    Human verifies the automated infrastructure work and completes two manual Supabase dashboard steps that cannot be automated via MCP.
  </action>
  <verify>
    1. Visit the Vercel deployment URL — should load the dashboard (or redirect to login if auth guard is active)
    2. Verify the sidebar and header still render correctly on app pages
    3. Visit /login, /signup, /forgot-password — should show placeholder auth pages with the centered layout
    4. **Manual step required**: Go to Supabase Dashboard -> Authentication -> Providers -> Email and set "Confirm Email" to OFF. This enables immediate access after signup (no verification email).
    5. **Manual step required**: Go to Supabase Dashboard -> Authentication -> URL Configuration and set:
       - Site URL: your Vercel deployment URL
       - Redirect URLs: your Vercel deployment URL followed by /** (wildcard)
  </verify>
  <done>Vercel deployment accessible, route groups rendering correctly, Supabase email confirmation disabled, redirect URLs configured</done>
</task>

</tasks>

<verification>
- Supabase project exists and responds to MCP queries
- `@supabase/supabase-js` and `@supabase/ssr` are in package.json dependencies
- Three Supabase client utilities exist: client.ts (browser), server.ts (server), proxy.ts (updateSession)
- `proxy.ts` at project root exports `proxy` function (not `middleware`)
- Route groups `(auth)` and `(app)` separate layouts correctly
- `npm run build` passes with zero errors
- Vercel deployment URL loads the app
- Environment variables are set in both .env.local and Vercel
</verification>

<success_criteria>
1. Supabase project is ACTIVE_HEALTHY and accessible via MCP tools
2. Next.js app builds without errors with all Supabase client libraries
3. proxy.ts exists at project root and exports the correct function
4. Route groups separate auth (centered layout) and app (sidebar layout)
5. All Phase 1 pages render correctly under the (app) route group
6. Application is deployed to Vercel and accessible from a live URL
7. Environment variables configured in .env.local and Vercel
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-data-layer/02-01-SUMMARY.md`
</output>
