---
phase: 01-frontend-design-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/page.tsx
  - src/app/contacts/page.tsx
  - src/app/contacts/[id]/page.tsx
  - src/app/deals/page.tsx
  - src/components/dashboard/metrics-cards.tsx
  - src/components/dashboard/pipeline-summary.tsx
  - src/components/dashboard/tasks-widget.tsx
  - src/components/dashboard/activity-feed.tsx
  - src/components/contacts/contacts-table.tsx
  - src/components/contacts/contacts-grid.tsx
  - src/components/contacts/view-toggle.tsx
  - src/components/contacts/contact-sheet.tsx
  - src/components/contacts/columns.tsx
  - src/components/deals/kanban-board.tsx
  - src/components/deals/kanban-column.tsx
  - src/components/deals/deal-card.tsx
  - src/components/contact-detail/contact-overview.tsx
  - src/components/contact-detail/linked-deals.tsx
  - src/components/contact-detail/linked-tasks.tsx
  - src/components/contact-detail/interaction-timeline.tsx
autonomous: true
requirements:
  - DSGN-02
  - DSGN-06
  - ARCH-06

must_haves:
  truths:
    - "Dashboard page shows 4 metrics cards, a pipeline summary chart, a tasks widget, and a recent activity feed"
    - "Contacts page has a sortable data table as default view with a toggle to switch to card grid view"
    - "Clicking a contacts table row opens a slide-over Sheet panel with contact quick-peek details and an 'Open full page' link"
    - "Deals page shows a Kanban board with stage columns, each column has a header with stage name, deal count, and total value"
    - "Deal cards on the Kanban board can be dragged between columns (stage changes reflected in local state)"
    - "Contact detail page shows contact overview at top, linked deals and tasks, and an interaction timeline below"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Dashboard page with 4 widget areas"
      min_lines: 20
    - path: "src/components/dashboard/metrics-cards.tsx"
      provides: "4 metric cards — pipeline value, active deals, won this month, tasks due"
      min_lines: 30
    - path: "src/components/contacts/contacts-table.tsx"
      provides: "TanStack Table with sorting for contacts"
      contains: "useReactTable"
    - path: "src/components/contacts/contact-sheet.tsx"
      provides: "Sheet slide-over panel for contact quick peek"
      contains: "SheetContent"
    - path: "src/components/deals/kanban-board.tsx"
      provides: "DndContext Kanban board for deal pipeline"
      contains: "DndContext"
    - path: "src/components/deals/deal-card.tsx"
      provides: "Minimal draggable deal card — name + value"
      contains: "useSortable"
    - path: "src/app/contacts/[id]/page.tsx"
      provides: "Contact detail page with overview + timeline"
      min_lines: 30
  key_links:
    - from: "src/app/page.tsx"
      to: "src/data/mock-deals.ts"
      via: "imports mockDeals for pipeline metrics"
      pattern: "mockDeals"
    - from: "src/components/contacts/contacts-table.tsx"
      to: "src/data/mock-contacts.ts"
      via: "imports mockContacts for table data"
      pattern: "mockContacts"
    - from: "src/components/deals/kanban-board.tsx"
      to: "src/data/mock-deals.ts"
      via: "imports mockDeals, groups by stage"
      pattern: "mockDeals"
    - from: "src/components/contacts/contact-sheet.tsx"
      to: "contacts-table.tsx"
      via: "table row click opens sheet with selected contact"
      pattern: "setSelectedContact"
    - from: "src/app/contacts/[id]/page.tsx"
      to: "src/data/mock-contacts.ts"
      via: "finds contact by params.id"
      pattern: "mockContacts.find"
---

<objective>
Build all four interactive prototype screens — Dashboard, Contacts List, Deal Pipeline Kanban, and Contact Detail — using the design system, components, and mock data established in Plan 01.

Purpose: These are the core screens the user must see and approve before any backend work begins. Each screen demonstrates the CRM's visual identity and interaction patterns with realistic healthcare data. This is the heart of Phase 1.

Output: Four fully-rendered, interactive prototype pages viewable at localhost — populated with mock data, with working sort, drag-and-drop, view toggles, and slide-over panels.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-frontend-design-ui/01-RESEARCH.md
@.planning/phases/01-frontend-design-ui/01-CONTEXT.md
@.planning/phases/01-frontend-design-ui/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Dashboard page and Contacts List page (table + grid + sheet)</name>
  <files>
    src/app/page.tsx
    src/components/dashboard/metrics-cards.tsx
    src/components/dashboard/pipeline-summary.tsx
    src/components/dashboard/tasks-widget.tsx
    src/components/dashboard/activity-feed.tsx
    src/app/contacts/page.tsx
    src/components/contacts/contacts-table.tsx
    src/components/contacts/contacts-grid.tsx
    src/components/contacts/view-toggle.tsx
    src/components/contacts/contact-sheet.tsx
    src/components/contacts/columns.tsx
  </files>
  <action>
    **Dashboard (src/app/page.tsx):**

    Build the dashboard as the landing page with the following layout (per research recommendation):

    Row 1 — Metrics bar (full width, 4 equal cards in a grid):
    - "Pipeline Value" — sum of all deal values from mockDeals where stage is not "Closed Lost"
    - "Active Deals" — count of deals not in "Closed Won" or "Closed Lost"
    - "Won This Month" — count of deals in "Closed Won" (hardcode a realistic number, e.g., 3, since all data is static)
    - "Tasks Due Today" — count of tasks with dueDate === today or overdue
    Each metric card: large number, label below, subtle icon. Use shadcn Card with padding. Style to feel like Linear's metric cards — minimal, clean, no borders on individual cards (use subtle background elevation).

    Row 2 — Pipeline summary (2/3 width) + Tasks widget (1/3 width):
    - Pipeline summary: A horizontal bar chart or visual representation of deals per stage. Show stage name, deal count, and total value per stage. Can be a simple styled series of horizontal bars (CSS, no chart library needed — keep it lightweight for prototype). Color the bars with varying opacities of the primary violet-indigo.
    - Tasks widget: List of upcoming/overdue tasks sorted by dueDate. Show task title, linked contact name (if any), due date, and priority badge. Use colored badges for priority (high = destructive color, medium = primary, low = muted). Overdue tasks get a visual flag (red dot or text). Limit to 5-7 most urgent tasks.

    Row 3 — Activity feed (full width):
    - Recent interactions from mockInteractions, sorted by date descending.
    - Each entry: icon (phone for call, mail for email, calendar for meeting, file-text for note), contact name, summary text, relative time ("2 hours ago", "Yesterday"). Use a simple list with subtle separator lines.
    - Limit to 8-10 most recent entries.

    Create each widget as a separate component in `src/components/dashboard/`:
    - `metrics-cards.tsx` — 4 metric cards
    - `pipeline-summary.tsx` — stage bar visualization
    - `tasks-widget.tsx` — upcoming tasks list
    - `activity-feed.tsx` — recent interactions list

    **Contacts List (src/app/contacts/page.tsx):**

    Build the contacts page with two views: table (default) and card grid, with a toggle to switch.

    Header area:
    - Page title "Contacts" on the left
    - View toggle button group on the right (Table icon / Grid icon) — uses `view-toggle.tsx`
    - "Add Contact" button (non-functional, just the button — no modal yet)

    Table view (`contacts-table.tsx`):
    - Use TanStack Table via `useReactTable` with `getCoreRowModel` and `getSortedRowModel`
    - Columns defined in `columns.tsx`: Avatar (initials circle), Name (sortable), Organization (sortable), Email, Title, Tags (as badges), Last Contact (sortable, formatted date)
    - Sortable columns have a sort indicator button (ArrowUpDown icon) in the header
    - Row click handler: opens contact-sheet slide-over (NOT navigates to detail page)
    - Style: shadcn Table component, striped rows or hover highlight, text-sm for density

    Card grid view (`contacts-grid.tsx`):
    - Grid of contact cards (3 columns on desktop, 2 on tablet, 1 on mobile)
    - Each card: avatar (initials in a colored circle), name, organization, title, tags (as small badges)
    - Card click also opens contact-sheet

    View toggle (`view-toggle.tsx`):
    - Two icon buttons (LayoutList, LayoutGrid from lucide-react)
    - Manages `view` state ("table" | "grid")
    - Active view button is highlighted with primary accent

    Contact sheet slide-over (`contact-sheet.tsx`):
    - shadcn Sheet with `side="right"` and `className="w-full sm:w-[480px]"`
    - Shows: contact name (large), title, organization, email, phone
    - Quick stats: deal count, last contact date
    - Tags as badges
    - "Open Full Profile" link (Next.js Link to `/contacts/[id]`)
    - Close button (X) or click outside to close

    All data comes from `mockContacts` imported from `src/data/mock-contacts.ts`. No API calls. No fetch(). Pure static data.

    IMPORTANT: The design should feel premium. Use the OKLCH design tokens. Cards should have subtle borders (border-border), hover states with slight background changes, and smooth transitions. Tables should be clean and scannable. Think Linear's issue list for the table, and Notion's gallery view for the card grid.
  </action>
  <verify>
    - `npm run build` completes with zero errors
    - Dashboard at `/` shows metrics cards, pipeline summary, tasks widget, and activity feed with mock data
    - Pipeline summary shows deal counts and values per stage
    - Tasks widget shows upcoming/overdue tasks with priority badges
    - Contacts page at `/contacts` shows data table with sortable columns
    - Clicking a column header sorts the table
    - View toggle switches between table and card grid
    - Clicking a table row or card opens the Sheet slide-over
    - Sheet shows contact details and "Open Full Profile" link
    - Sheet closes when clicking outside or pressing Escape
  </verify>
  <done>
    Dashboard renders 4 metrics, pipeline summary, tasks, and activity feed. Contacts page has working table-to-grid toggle, sortable columns, and a slide-over Sheet for contact quick peek. All data is static from mock files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Deal Pipeline Kanban and Contact Detail page</name>
  <files>
    src/app/deals/page.tsx
    src/components/deals/kanban-board.tsx
    src/components/deals/kanban-column.tsx
    src/components/deals/deal-card.tsx
    src/app/contacts/[id]/page.tsx
    src/components/contact-detail/contact-overview.tsx
    src/components/contact-detail/linked-deals.tsx
    src/components/contact-detail/linked-tasks.tsx
    src/components/contact-detail/interaction-timeline.tsx
  </files>
  <action>
    **Deal Pipeline Kanban (src/app/deals/page.tsx):**

    Build the Kanban board for the deal pipeline using @dnd-kit/core and @dnd-kit/sortable.

    Page layout:
    - Page title "Deal Pipeline" with total pipeline value displayed on the right
    - "Add Deal" button (non-functional)
    - Horizontally scrollable Kanban board below

    Kanban board (`kanban-board.tsx`):
    - `DndContext` wrapper with `PointerSensor` and `closestCorners` collision detection
    - Initialize deals state from `mockDeals` with `useState`
    - Group deals by `stage` field, one column per stage from `PIPELINE_STAGES`
    - `DragOverlay` renders a ghost `DealCard` of the active item during drag
    - `onDragStart`: set activeId state
    - `onDragEnd`: update the dragged deal's `stage` field to the target column's stage. Use functional setState: `setDeals(prev => prev.map(d => d.id === activeId ? {...d, stage: targetStage} : d))`. This makes drag-and-drop actually work with local state.
    - `onDragOver`: can handle cross-container visual feedback (optional, nice-to-have)

    Kanban column (`kanban-column.tsx`):
    - Each column: flex-shrink-0, width 280px, rounded card background (card color from design tokens)
    - Column header: stage name (bold), deal count badge, total column value formatted as currency
    - `SortableContext` wrapping the deal cards with `verticalListSortingStrategy`
    - `useDroppable` on the column container so deals can be dropped into empty columns
    - Empty columns should show a subtle "No deals" placeholder

    Deal card (`deal-card.tsx`):
    - Minimal per locked decision: deal name + value only
    - Use `useSortable` hook from `@dnd-kit/sortable`
    - Apply `transform` and `transition` from useSortable for smooth drag animation
    - Small card with: deal name (text-sm, font-medium), value below (text-xs, text-muted-foreground, formatted as $XXX,XXX)
    - Organization name as a tiny label (text-xs, text-muted-foreground) — optional addition to name+value since it aids scannability
    - Subtle hover state, cursor-grab on hover, cursor-grabbing while dragging
    - Style: shadcn Card aesthetic but smaller — padding-3, subtle border, rounded-md

    **Contact Detail Page (src/app/contacts/[id]/page.tsx):**

    Build the full contact detail page — the "deep editing" view per locked decision (as opposed to the quick-peek Sheet).

    Page layout — overview-centered per locked decision:

    Top section — Contact Overview (`contact-overview.tsx`):
    - Large avatar (initials in colored circle, 64px)
    - Contact name (text-2xl, font-bold), title, organization (as a link placeholder)
    - Contact info row: email, phone, tags as badges
    - "Edit" and "Delete" buttons (non-functional placeholders)
    - "Back to Contacts" link

    Middle section — Linked entities:
    - Linked Deals (`linked-deals.tsx`): Cards showing deals associated with this contact from mockDeals. Each card: deal name, value, stage badge, expected close date. If no linked deals, show empty state.
    - Linked Tasks (`linked-tasks.tsx`): Cards showing tasks associated with this contact from mockTasks. Each card: task title, priority badge, due date, status. Overdue tasks visually flagged.

    Bottom section — Interaction Timeline (`interaction-timeline.tsx`):
    - Chronological timeline of interactions from mockInteractions filtered by contactId
    - Each entry: type icon (Phone, Mail, Calendar, FileText from lucide-react), date, summary text
    - Visual timeline line connecting entries (vertical line with dots at each entry)
    - If no interactions, show empty state

    Data loading: Use `params.id` to find the contact from `mockContacts.find(c => c.id === params.id)`. Filter related deals, tasks, and interactions by contactId. If contact not found, show a "Contact not found" message.

    IMPORTANT: Keep cards minimal. The Kanban should feel fast and scannable — like Trello or Linear's board view. The contact detail should feel like a rich profile page — like HubSpot's contact record. The prototype is data-static but must feel real and complete.
  </action>
  <verify>
    - `npm run build` completes with zero errors
    - Deals page at `/deals` shows a Kanban board with columns for each pipeline stage
    - Each column header shows stage name, deal count, and total value
    - Deal cards show deal name and value
    - Dragging a card to a different column moves it (stage changes in local state)
    - Column totals and counts update after a drag
    - Contact detail at `/contacts/c-001` (or any valid mock ID) shows contact overview, linked deals, linked tasks, and interaction timeline
    - Linked deals and tasks sections show relevant data or empty state
    - Interaction timeline shows chronological entries with type icons
    - "Back to Contacts" link navigates back to `/contacts`
  </verify>
  <done>
    Deal Pipeline Kanban board renders with draggable deal cards across pipeline stage columns, with working cross-column drag-and-drop and live count/value updates. Contact Detail page shows a complete profile with overview, linked deals, linked tasks, and interaction timeline. All data is static from mock files.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. Dashboard (`/`) — 4 metric cards visible, pipeline summary with stage bars, tasks widget, activity feed
3. Contacts (`/contacts`) — data table loads with sortable columns, view toggle switches to card grid, row click opens Sheet panel
4. Deals (`/deals`) — Kanban board with 6 stage columns, cards show name + value, drag-and-drop works between columns
5. Contact Detail (`/contacts/[id]`) — overview with contact info, linked deals, linked tasks, interaction timeline
6. All data comes from `src/data/mock-*.ts` files — no API calls, no fetch(), no server actions
7. All screens use the OKLCH dark design tokens consistently — violet-indigo accents, near-black backgrounds
8. Navigation between all screens works via sidebar
</verification>

<success_criteria>
- All 4 key prototype screens render with realistic mock data and are navigable
- Dashboard shows pipeline overview, tasks, and activity — a sales user's daily view
- Contacts list supports table/grid toggle and slide-over quick peek
- Deal pipeline Kanban supports drag-and-drop between stages with live state updates
- Contact detail shows a complete relationship view (overview + deals + tasks + timeline)
- Design feels premium — consistent dark theme, smooth interactions, clean typography
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-design-ui/01-02-SUMMARY.md`
</output>
