---
phase: 03-integration-features
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/lib/queries/interactions.ts
  - src/lib/queries/tasks.ts
  - src/lib/queries/dashboard.ts
  - src/lib/actions/interactions.ts
  - src/lib/actions/tasks.ts
  - src/components/interactions/interaction-form.tsx
  - src/components/interactions/interaction-list.tsx
  - src/components/tasks/task-form.tsx
  - src/components/tasks/task-list.tsx
  - src/components/tasks/task-filters.tsx
  - src/components/dashboard/metrics-cards.tsx
  - src/components/dashboard/pipeline-summary.tsx
  - src/components/dashboard/tasks-widget.tsx
  - src/components/dashboard/activity-feed.tsx
  - src/components/contact-detail/interaction-timeline.tsx
  - src/components/contact-detail/linked-tasks.tsx
  - src/components/layout/app-sidebar.tsx
  - src/app/(app)/interactions/page.tsx
  - src/app/(app)/tasks/page.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/deals/[id]/page.tsx
  - src/app/(app)/contacts/[id]/page.tsx
autonomous: true
requirements:
  - INTR-01
  - INTR-02
  - INTR-03
  - INTR-04
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - PROC-03

must_haves:
  truths:
    - "User can log an interaction (call, email, meeting, note) linked to a contact and/or deal"
    - "User can edit and soft-delete interactions"
    - "Interactions display as a chronological timeline on contact detail pages"
    - "Interactions display as a chronological timeline on deal detail pages"
    - "User can create a task with title, description, due date, and link to contact and/or deal"
    - "User can edit, complete, and soft-delete tasks"
    - "Overdue tasks are visually flagged (red text on due date)"
    - "Task list view filters by status: pending, completed, overdue"
    - "Dashboard shows pipeline value by stage (bar chart)"
    - "Dashboard shows tasks due today and overdue task count"
    - "Dashboard shows recent activity feed (latest interactions)"
    - "Dashboard shows deal count and total pipeline value"
    - "Dashboard is the landing page after login"
  artifacts:
    - path: "src/lib/queries/interactions.ts"
      provides: "Interaction read queries"
      exports: ["getInteractions", "getInteractionsByContact", "getInteractionsByDeal"]
    - path: "src/lib/actions/interactions.ts"
      provides: "Interaction CRUD Server Actions"
      exports: ["createInteraction", "updateInteraction", "deleteInteraction"]
    - path: "src/lib/queries/tasks.ts"
      provides: "Task read queries with overdue detection"
      exports: ["getTasks", "getTasksByContact", "getTasksByDeal"]
    - path: "src/lib/actions/tasks.ts"
      provides: "Task CRUD + complete Server Actions"
      exports: ["createTask", "updateTask", "completeTask", "deleteTask"]
    - path: "src/lib/queries/dashboard.ts"
      provides: "Dashboard aggregation queries"
      exports: ["getDashboardMetrics"]
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Dashboard landing page with live metrics"
    - path: "src/app/(app)/tasks/page.tsx"
      provides: "Tasks list page with status filtering"
    - path: "src/app/(app)/interactions/page.tsx"
      provides: "Interactions feed page"
  key_links:
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "src/lib/queries/dashboard.ts"
      via: "Server Component direct import"
      pattern: "getDashboardMetrics"
    - from: "src/components/contact-detail/interaction-timeline.tsx"
      to: "src/lib/queries/interactions.ts"
      via: "Props from Server Component page"
      pattern: "getInteractionsByContact"
    - from: "src/app/(app)/tasks/page.tsx"
      to: "src/lib/queries/tasks.ts"
      via: "Server Component direct import"
      pattern: "getTasks"
    - from: "src/components/dashboard/pipeline-summary.tsx"
      to: "src/lib/queries/deals.ts"
      via: "Data passed from dashboard page"
      pattern: "getDashboardMetrics"
---

<objective>
Wire interactions, tasks, and dashboard to live Supabase data. Build interaction CRUD with chronological timelines on contact and deal detail pages. Build task CRUD with overdue flagging and status filtering. Wire all four dashboard widgets (pipeline metrics, task counts, activity feed, deal summary) to live aggregation queries. Complete the code review requirement.

Purpose: This is the final integration plan — after this, every CRM entity is functional with live data and the dashboard provides real-time operational visibility. The application becomes fully usable for daily sales and account management work.

Output: Working interactions, tasks, and dashboard — all wired to Supabase. Complete CRM application with all Phase 3 features functional.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-integration-features/03-RESEARCH.md
@.planning/phases/03-integration-features/03-CONTEXT.md
@.planning/phases/03-integration-features/03-01-SUMMARY.md
@.planning/phases/03-integration-features/03-02-SUMMARY.md
@src/types/database.ts
@src/data/mock-interactions.ts
@src/data/mock-tasks.ts
@src/components/dashboard/metrics-cards.tsx
@src/components/dashboard/pipeline-summary.tsx
@src/components/dashboard/tasks-widget.tsx
@src/components/dashboard/activity-feed.tsx
@src/components/contact-detail/interaction-timeline.tsx
@src/components/contact-detail/linked-tasks.tsx
@src/app/(app)/dashboard/page.tsx
@src/app/(app)/tasks/page.tsx
@src/app/(app)/interactions/page.tsx
@src/app/(app)/deals/[id]/page.tsx
@src/app/(app)/contacts/[id]/page.tsx
@src/lib/types/app.ts
@src/lib/queries/account.ts
@src/lib/queries/deals.ts
@src/lib/queries/pipeline-stages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Interactions CRUD — queries, actions, forms, timelines on contact and deal detail pages</name>
  <files>
    src/lib/queries/interactions.ts
    src/lib/actions/interactions.ts
    src/lib/types/app.ts
    src/components/interactions/interaction-form.tsx
    src/components/interactions/interaction-list.tsx
    src/components/contact-detail/interaction-timeline.tsx
    src/app/(app)/interactions/page.tsx
    src/app/(app)/contacts/[id]/page.tsx
    src/app/(app)/deals/[id]/page.tsx
  </files>
  <action>
    **1. Update `src/lib/types/app.ts` — add interaction and task types:**
    - `InteractionRow` = `Tables<'interactions'>`
    - `InteractionWithRelations` = interaction row + `{ contact?: { id: string; first_name: string; last_name: string }; deal?: { id: string; title: string } }`
    - `TaskRow` = `Tables<'tasks'>`
    - `TaskWithRelations` = task row + `{ contact?: { id: string; first_name: string; last_name: string }; deal?: { id: string; title: string }; isOverdue: boolean }`

    **2. Create `src/lib/queries/interactions.ts`:**
    - `getInteractions(options?: { page?: number; pageSize?: number })` — returns `PaginatedResult<InteractionWithRelations>`
    - Query: `supabase.from('interactions').select('*, contacts(id, first_name, last_name), deals(id, title)', { count: 'exact' }).is('deleted_at', null).order('occurred_at', { ascending: false }).range(from, to)`
    - `getInteractionsByContact(contactId: string)` — all interactions for a contact, ordered by `occurred_at DESC`
    - `getInteractionsByDeal(dealId: string)` — all interactions for a deal, ordered by `occurred_at DESC`
    - `getRecentInteractions(limit?: number)` — latest N interactions across all records (for dashboard activity feed)

    **3. Create `src/lib/actions/interactions.ts`:**
    - `'use server'` directive
    - Zod schema: `type` enum `['call', 'email', 'meeting', 'note']` required, `subject` string optional, `body` text optional, `occurred_at` datetime required (default now), `duration_mins` number optional, `contact_id` UUID optional, `deal_id` UUID optional
    - `createInteraction(_prevState: ActionState, formData: FormData)`:
      1. Validate with Zod
      2. `getAccountId` for `account_id`
      3. Insert with `account_id`, `user_id` (current user)
      4. If contact_id: get contact's org_id from contact_organizations, set `organization_id`
      5. `revalidatePath('/interactions')`, also revalidate contact/deal detail paths if linked
    - `updateInteraction(_prevState: ActionState, formData: FormData)` — update, revalidatePath
    - `deleteInteraction(id: string)` — soft-delete, revalidatePath

    **4. Create `src/components/interactions/interaction-form.tsx`:**
    - Client Component with `useActionState`
    - Quick-log approach: compact form with type select (call/email/meeting/note), subject line, body textarea, occurred_at datetime, duration (only for calls/meetings)
    - Contact and deal selects populated via props (lists from parent Server Component)
    - Wrap in Dialog (modal) — interactions are quick-log items, not full-page forms
    - Edit mode: pre-populate all fields

    **5. Create `src/components/interactions/interaction-list.tsx`:**
    - Server Component rendering interaction cards
    - Each card: type icon (Phone/Mail/Users/StickyNote from lucide-react), subject, contact name, deal title, occurred_at formatted, body preview
    - Edit/delete actions on each card

    **6. Update `src/components/contact-detail/interaction-timeline.tsx`:**
    - Replace mock data rendering with real `InteractionWithRelations[]` props
    - Maintain existing timeline visual pattern: vertical line + icon nodes + content cards
    - Each node: type icon, subject/title, occurred_at relative date, body preview, edit/delete buttons
    - "Log Interaction" button at top opens interaction form modal (pre-filled with this contact's ID)
    - Handle empty state gracefully (no interactions logged yet)

    **7. Wire interactions into contact and deal detail pages:**
    - `src/app/(app)/contacts/[id]/page.tsx`: fetch `getInteractionsByContact(id)`, pass to InteractionTimeline in the Interactions tab
    - `src/app/(app)/deals/[id]/page.tsx`: fetch `getInteractionsByDeal(id)`, pass to InteractionTimeline section (replace empty state placeholder from 03-02)
    - Both pages: "Log Interaction" button that opens interaction form pre-linked to the entity

    **8. Wire `src/app/(app)/interactions/page.tsx`:**
    - Server Component replacing existing stub
    - Fetch `getInteractions({ page })` with pagination
    - Render: page header with "Log Interaction" button, InteractionList, Pagination
    - This is a global feed of all interactions across all contacts/deals

    **9. Remove mock data imports:**
    - Remove all imports from `src/data/mock-interactions.ts` in interaction-related components
  </action>
  <verify>
    - Navigate to `/interactions` — shows interaction feed (initially empty, create one to verify)
    - Log a new interaction linked to a contact — appears on contact detail timeline
    - Log a new interaction linked to a deal — appears on deal detail timeline
    - Edit an interaction — changes persist
    - Delete an interaction — soft-deleted, disappears
    - Contact detail Interactions tab shows chronological timeline
    - Deal detail shows interaction timeline section
    - `npx next build` succeeds
  </verify>
  <done>
    Interactions have full CRUD. Chronological timeline displays on both contact detail and deal detail pages. Global interaction feed page works with pagination. Quick-log modal form for fast interaction entry. No mock data imports remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tasks CRUD — queries, actions, overdue flagging, status filtering, task list page</name>
  <files>
    src/lib/queries/tasks.ts
    src/lib/actions/tasks.ts
    src/components/tasks/task-form.tsx
    src/components/tasks/task-list.tsx
    src/components/tasks/task-filters.tsx
    src/components/contact-detail/linked-tasks.tsx
    src/components/layout/app-sidebar.tsx
    src/app/(app)/tasks/page.tsx
    src/app/(app)/contacts/[id]/page.tsx
    src/app/(app)/deals/[id]/page.tsx
  </files>
  <action>
    **1. Create `src/lib/queries/tasks.ts`:**
    - `getTasks(options?: { status?: 'pending' | 'completed' | 'overdue'; page?: number; pageSize?: number })` — returns `PaginatedResult<TaskWithRelations>`
    - Query: `supabase.from('tasks').select('*, contacts(id, first_name, last_name), deals(id, title)', { count: 'exact' }).is('deleted_at', null).order('due_date', { ascending: true }).range(from, to)`
    - Status filtering logic (application-side after fetch, or via query):
      - `pending`: `.eq('is_complete', false)` — then optionally exclude overdue in app
      - `completed`: `.eq('is_complete', true)`
      - `overdue`: `.eq('is_complete', false)` + filter `due_date < now()` — use `.lt('due_date', new Date().toISOString().split('T')[0])` or filter application-side
    - Compute `isOverdue` on each task: `!task.is_complete && task.due_date && new Date(task.due_date) < new Date()`
    - `getTasksByContact(contactId: string)` — tasks for a contact
    - `getTasksByDeal(dealId: string)` — tasks for a deal
    - `getTasksDueToday()` — tasks where `due_date = today` and `is_complete = false`
    - `getOverdueTaskCount()` — count of overdue incomplete tasks (for sidebar badge)

    **2. Create `src/lib/actions/tasks.ts`:**
    - `'use server'` directive
    - Zod schema: `title` required, `description` optional, `due_date` date string optional, `priority` enum `['low', 'medium', 'high']` optional, `contact_id` UUID optional, `deal_id` UUID optional
    - `createTask(_prevState: ActionState, formData: FormData)`:
      1. Validate with Zod
      2. `getAccountId`
      3. Insert with `account_id`, `created_by`, `assignee_id` = current user
      4. If contact_id provided, get contact's org_id and set `organization_id`
      5. `revalidatePath('/tasks')`
    - `updateTask(_prevState: ActionState, formData: FormData)` — update task, revalidatePath
    - `completeTask(taskId: string)` — toggle: if incomplete set `is_complete = true, completed_at = now()`, if complete set `is_complete = false, completed_at = null`. `revalidatePath`
    - `deleteTask(id: string)` — soft-delete, revalidatePath

    **3. Create `src/components/tasks/task-form.tsx`:**
    - Client Component with `useActionState`
    - Fields: title (required), description (textarea), due_date (date input), priority (select: low/medium/high), contact (select), deal (select)
    - Contact and deal lists passed as props
    - Wrap in Sheet (slide-over) consistent with other entity forms
    - Edit mode: pre-populate all fields

    **4. Create `src/components/tasks/task-list.tsx`:**
    - Server Component rendering task rows/cards
    - Props: `tasks: TaskWithRelations[]`
    - Each row: checkbox (complete toggle via `completeTask` action), title, due date, priority badge, linked contact name, linked deal title
    - Overdue visual: if `isOverdue`, due date text uses `text-destructive` (red) class, plus a small "Overdue" badge
    - Completed tasks: strikethrough title, muted text
    - Edit/delete actions on each row

    **5. Create `src/components/tasks/task-filters.tsx`:**
    - Client Component for status filtering
    - Tab-style filter: All | Pending | Completed | Overdue
    - Updates URL searchParam `?status=pending|completed|overdue`
    - Uses existing shadcn Tabs or button group pattern
    - Submit-based (locked decision) — clicking a tab navigates with URL update

    **6. Update `src/components/contact-detail/linked-tasks.tsx`:**
    - Replace mock data rendering with real `TaskWithRelations[]` props
    - Show task title, due date (red if overdue), completion status checkbox
    - "Add Task" button opens task form pre-linked to this contact
    - Handle empty state

    **7. Wire task sections into detail pages:**
    - `src/app/(app)/contacts/[id]/page.tsx`: fetch `getTasksByContact(id)`, pass to LinkedTasks in Tasks tab
    - `src/app/(app)/deals/[id]/page.tsx`: fetch `getTasksByDeal(id)`, pass to tasks section (replace empty placeholder from 03-02)

    **8. Wire `src/app/(app)/tasks/page.tsx`:**
    - Server Component replacing existing stub
    - Read `searchParams` for `status` and `page`
    - Call `getTasks({ status, page })`
    - Render: page header with "New Task" button, TaskFilters, TaskList, Pagination

    **9. Add overdue badge to sidebar:**
    - Update `src/components/layout/app-sidebar.tsx`
    - Fetch overdue task count (via a lightweight server query or pass as layout-level data)
    - Show red badge on Tasks nav item if overdue count > 0
    - Pattern: small red circle with count number next to "Tasks" label

    **10. Remove mock data imports:**
    - Remove all imports from `src/data/mock-tasks.ts` in task-related components
  </action>
  <verify>
    - Navigate to `/tasks` — shows task list (initially empty or with any seeded data)
    - Create a new task with due date — appears in list
    - Complete a task — checkbox toggles, completion persisted
    - Set a task due date to yesterday — shows overdue visual (red text, "Overdue" badge)
    - Filter by status: Pending shows only incomplete non-overdue, Completed shows done, Overdue shows past-due incomplete
    - Pagination works on task list
    - Contact detail Tasks tab shows linked tasks
    - Deal detail shows linked tasks section
    - Sidebar Tasks item shows overdue count badge
    - Edit and delete tasks work
    - `npx next build` succeeds
  </verify>
  <done>
    Tasks have full CRUD with completion toggle, overdue visual flagging, and status filtering. Task list page with filter tabs and pagination works. Tasks display on contact and deal detail pages. Sidebar shows overdue count badge. No mock data imports remain.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dashboard wiring — live metrics, pipeline chart, task counts, activity feed, code review</name>
  <files>
    src/lib/queries/dashboard.ts
    src/components/dashboard/metrics-cards.tsx
    src/components/dashboard/pipeline-summary.tsx
    src/components/dashboard/tasks-widget.tsx
    src/components/dashboard/activity-feed.tsx
    src/app/(app)/dashboard/page.tsx
  </files>
  <action>
    **1. Create `src/lib/queries/dashboard.ts`:**
    - `getDashboardMetrics()` — single function returning all dashboard data
    - Use `Promise.all` for parallel queries:
      a. Deals: `supabase.from('deals').select('value, stage_id, pipeline_stages(name, display_order, is_won, is_lost, color)').is('deleted_at', null)`
      b. Tasks: `supabase.from('tasks').select('id, due_date, is_complete, title').is('deleted_at', null).eq('is_complete', false)`
      c. Recent activity: `supabase.from('interactions').select('*, contacts(first_name, last_name), deals(title)').is('deleted_at', null).order('occurred_at', { ascending: false }).limit(10)`
      d. Total deals count + total value: computed from deals query result
    - Compute from results:
      - `pipelineByStage`: group deals by stage, sum values, include stage name/color/order
      - `totalDeals`: count of non-closed deals
      - `totalPipelineValue`: sum of all non-closed deal values
      - `tasksDueToday`: filter tasks where `due_date` is today
      - `overdueTaskCount`: filter tasks where `due_date < today`
      - `recentActivity`: latest 10 interactions

    **2. Update `src/components/dashboard/metrics-cards.tsx`:**
    - Props change: accept real data `{ totalDeals: number; totalPipelineValue: number; tasksDueToday: number; overdueTaskCount: number }`
    - Remove mock data references
    - Keep existing card layout: 4 metric cards with icons and large numbers
    - Format pipeline value as currency (USD)
    - Overdue count: use `text-destructive` if > 0

    **3. Update `src/components/dashboard/pipeline-summary.tsx`:**
    - Props change: accept `pipelineByStage: { name: string; count: number; value: number; color: string; displayOrder: number }[]`
    - Keep existing CSS-only bar chart pattern from Phase 1 (div width% with color scale)
    - Use stage colors from DB (`pipeline_stages.color` hex values)
    - Each bar: stage name, deal count, value, proportional width
    - Sort by `displayOrder`

    **4. Update `src/components/dashboard/tasks-widget.tsx`:**
    - Props change: accept real task data `{ tasksDueToday: number; overdueCount: number; upcomingTasks: TaskRow[] }`
    - Remove mock data references
    - Show: "X tasks due today" + "Y overdue" (red if Y > 0)
    - List upcoming tasks (next few due) with due dates
    - Link to `/tasks` for full view

    **5. Update `src/components/dashboard/activity-feed.tsx`:**
    - Props change: accept `recentActivity: InteractionWithRelations[]`
    - Remove mock data references
    - Keep existing timeline/feed layout
    - Each item: type icon, subject, contact name, relative time (e.g., "2 hours ago")
    - Link to contact detail or deal detail
    - Handle empty state: "No recent activity" message

    **6. Wire `src/app/(app)/dashboard/page.tsx`:**
    - Server Component replacing mock data version
    - Call `getDashboardMetrics()`
    - Pass computed data to: MetricsCards, PipelineSummary, TasksWidget, ActivityFeed
    - Locked decision: Dashboard IS the landing page after login (DASH-05) — already configured in proxy.ts redirect

    **7. Remove all remaining mock data imports:**
    - Verify NO component anywhere imports from `src/data/mock-*.ts`
    - The mock data files can remain in the repo (cleanup is Phase 4 concern) but no active code should reference them

    **8. PROC-03: Code review using code-reviewer agent:**
    - After all dashboard wiring is complete and `npx next build` passes, run the code-reviewer agent on the Phase 3 changes
    - Focus areas: Server Action security (auth checks, input validation), RLS reliance (every query filtered by deleted_at), type safety (no `any` types), consistent patterns
    - Address any critical findings before marking plan complete
    - Command: invoke code-reviewer agent with scope limited to `src/lib/queries/`, `src/lib/actions/`, and new/modified components
  </action>
  <verify>
    - Navigate to `/dashboard` after login — shows all 4 widgets with live data
    - MetricsCards: deal count and pipeline value match actual deals in Supabase
    - PipelineSummary: bars match deal distribution across stages
    - TasksWidget: shows correct count of tasks due today and overdue tasks
    - ActivityFeed: shows recent interactions (or empty state if none exist)
    - Create a deal → dashboard pipeline updates on refresh
    - Create a task due today → dashboard task count increments
    - Log an interaction → activity feed shows it
    - `npx next build` succeeds with no type errors
    - No imports from `src/data/mock-*.ts` remain in any active component
    - Code review completed with no critical findings
  </verify>
  <done>
    Dashboard is fully wired with live Supabase data: pipeline metrics, task counts, and activity feed all display real data. All four dashboard widgets (MetricsCards, PipelineSummary, TasksWidget, ActivityFeed) render from live queries. Mock data imports are completely removed from active code. Code review completed per PROC-03. The entire Phase 3 application is functional for daily CRM use.
  </done>
</task>

</tasks>

<verification>
- `npx next build` compiles without errors
- Dashboard renders with live data from Supabase
- Interactions CRUD works: create, edit, delete, timeline display on contact and deal pages
- Tasks CRUD works: create, edit, complete, delete, overdue flagging, status filtering
- No imports from any `src/data/mock-*.ts` file remain in active components
- Code review agent has reviewed Phase 3 implementation (PROC-03)
- Login redirects to dashboard (DASH-05)
- All five CRM entities (organizations, contacts, deals, interactions, tasks) fully operational with live data
</verification>

<success_criteria>
1. Interactions: create, edit, delete, timeline on contact detail, timeline on deal detail
2. Tasks: create, edit, complete, delete, overdue flagging, status filter (pending/completed/overdue)
3. Dashboard: pipeline value by stage, deal count + total value, tasks due today + overdue count, recent activity feed
4. Dashboard is landing page after login
5. Sidebar shows overdue task badge count
6. All mock data imports removed from active components
7. Code review completed (PROC-03) with no critical findings
</success_criteria>

<output>
After completion, create `.planning/phases/03-integration-features/03-03-SUMMARY.md`
</output>
