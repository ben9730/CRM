---
phase: 04-polish-production
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/app/(app)/contacts/page.tsx
  - src/app/(app)/organizations/page.tsx
  - src/app/(app)/deals/page.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - PROC-04

must_haves:
  truths:
    - "Application builds without errors on Vercel"
    - "All pages render correctly at 1280px desktop, 768px tablet, and 375px mobile"
    - "CSV export, global search, avatar dropdown, and task auto-linking all work on production deployment"
    - "No console errors or broken layouts on any core page"
  artifacts:
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Dashboard page rendering without errors"
    - path: "src/app/(app)/contacts/page.tsx"
      provides: "Contacts page with export button rendering correctly"
    - path: "src/app/(app)/deals/page.tsx"
      provides: "Deals page with export button rendering correctly"
  key_links:
    - from: "Vercel deployment"
      to: "Supabase production"
      via: "Environment variables"
      pattern: "NEXT_PUBLIC_SUPABASE"
---

<objective>
Verify the production deployment is fully functional, all new Phase 4 features work on the live URL, and responsive design is polished across all breakpoints.

Purpose: Final quality gate before declaring the application production-ready and Phase 4 complete.
Output: Verified production deployment, responsive polish applied, all Phase 4 features confirmed working.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-production/04-01-SUMMARY.md
@.planning/phases/04-polish-production/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build verification and responsive polish</name>
  <files>
    src/app/(app)/contacts/page.tsx
    src/app/(app)/organizations/page.tsx
    src/app/(app)/deals/page.tsx
    src/app/(app)/dashboard/page.tsx
  </files>
  <action>
**Build Verification:**
1. Run `npx next build` and confirm zero errors.
2. Check for any TypeScript errors or warnings that should be resolved.

**Responsive Polish (DSGN-04 follow-through):**
Use Playwright MCP browser tools to check all core pages at three breakpoints: 1280px (desktop), 768px (tablet), 375px (mobile).

Pages to check:
- `/dashboard` — metric cards, pipeline summary, tasks widget, activity feed
- `/contacts` — contacts list/table, export button, search, pagination
- `/organizations` — org list, export button
- `/deals` — Kanban board, export button, deal cards
- `/tasks` — task list, filters
- `/search?q=test` — search results grouped sections

For each page at each breakpoint:
1. Take a screenshot or snapshot
2. Identify any layout breaks, overflow issues, or elements that don't adapt
3. Fix issues found — typically Tailwind responsive classes (sm:, md:, lg: prefixes), flex-wrap, hidden/block toggles

Common responsive issues to check:
- Kanban board horizontal scroll on mobile (should be scrollable, not cramped)
- Export buttons wrapping correctly next to create buttons on small screens
- Search results page sections stacking vertically on mobile
- Avatar dropdown not clipped on mobile
- Dashboard metric cards stacking 1-column on mobile, 2-column on tablet

Apply fixes directly. Focus on functional layout, not pixel-perfect — the design system handles most styling via Tailwind utility classes.
  </action>
  <verify>
    <automated>cd "D:/claude code/CRM" && npx next build 2>&1 | tail -5</automated>
    <manual>Check core pages at 1280px, 768px, 375px — no layout breaks or overflow issues</manual>
  </verify>
  <done>Production build passes. All core pages render correctly at desktop, tablet, and mobile breakpoints. No layout breaks, overflow, or clipped elements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final production verification and sign-off</name>
  <files>src/app/(app)/dashboard/page.tsx</files>
  <action>
Human verification checkpoint. Complete Phase 4 implementation: CSV export for contacts/organizations/deals, global search page, avatar profile dropdown, task auto-linking, security review, E2E test suite, and responsive polish. The application is production-ready on Vercel + Supabase.

**Visit the live deployment URL and verify each feature:**

1. **Dashboard** (https://healthcrm-tawny.vercel.app/dashboard):
   - Metric cards display pipeline value, deal count, tasks due, overdue count
   - Pipeline summary bar chart renders
   - Activity feed shows recent interactions

2. **CSV Export:**
   - Go to /contacts — click "Export CSV" — CSV downloads with contact data
   - Go to /organizations — click "Export CSV" — CSV downloads with org data
   - Go to /deals — click "Export CSV" — CSV downloads with deal data including stage names
   - Open a CSV in Excel/Sheets — verify columns are correct and no garbled characters

3. **Global Search:**
   - Type a search query in the header search bar and press Enter
   - Verify you land on /search?q=... with results grouped by Contacts, Organizations, Deals

4. **Avatar Dropdown:**
   - Click the avatar circle in the top-right
   - Verify dropdown shows your name, email, and a "Log out" button
   - Click "Log out" — verify you are redirected to the login page

5. **Task Auto-Linking:**
   - Go to a contact detail page — click "Add Task"
   - Verify the contact is pre-selected in the task form
   - Go to a deal detail page — click "Add Task"
   - Verify the deal is pre-selected in the task form

6. **Responsive:**
   - Resize browser to ~768px — verify layout adapts (sidebar collapses, cards reflow)
   - Resize to ~375px — verify mobile layout works (no overflow, readable content)

7. **No Errors:**
   - Open browser DevTools console — verify no red errors on any page

Resume signal: Type "approved" if everything works, or list issues to fix.
  </action>
  <verify>
    <automated>cd "D:/claude code/CRM" && npx next build 2>&1 | tail -5</automated>
    <manual>User visually verifies all features on live deployment at three breakpoints</manual>
  </verify>
  <done>User has approved all Phase 4 features on the live deployment. Application is production-ready: CSV export works, search works, avatar dropdown works, task auto-linking works, no console errors, responsive layout verified.</done>
</task>

</tasks>

<verification>
1. `npx next build` passes without errors
2. All pages render correctly at 1280px, 768px, 375px
3. User confirms all Phase 4 features work on live deployment
4. No console errors on any page
</verification>

<success_criteria>
- Production build clean
- Responsive layout verified at all three breakpoints
- User has approved all Phase 4 features on live deployment
- Application declared production-ready
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-production/04-03-SUMMARY.md`
</output>
