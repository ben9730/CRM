---
phase: 04-polish-production
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - playwright.config.ts
  - e2e/global-setup.ts
  - e2e/contacts.spec.ts
  - e2e/organizations.spec.ts
  - e2e/deals.spec.ts
  - e2e/tasks.spec.ts
  - e2e/interactions.spec.ts
  - e2e/dashboard.spec.ts
  - .gitignore
  - .env.test.local
autonomous: false
requirements:
  - PROC-04

must_haves:
  truths:
    - "Security review has been run using security-reviewer agent and Supabase MCP advisors with no critical findings unresolved"
    - "E2E tests cover CRUD operations for contacts, organizations, deals, tasks, and interactions"
    - "E2E tests cover Kanban drag-and-drop deal stage changes"
    - "E2E tests cover dashboard loading with live metrics"
    - "E2E tests run against live Vercel+Supabase deployment and pass"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright configuration targeting live deployment"
      contains: "defineConfig"
    - path: "e2e/global-setup.ts"
      provides: "Authentication setup saving storageState"
      contains: "storageState"
    - path: "e2e/contacts.spec.ts"
      provides: "Contacts CRUD E2E tests"
      contains: "test.describe"
    - path: "e2e/deals.spec.ts"
      provides: "Deals CRUD + Kanban DnD E2E tests"
      contains: "dragTo"
    - path: "e2e/dashboard.spec.ts"
      provides: "Dashboard metrics E2E test"
      contains: "dashboard"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/global-setup.ts"
      via: "globalSetup config"
      pattern: "globalSetup.*global-setup"
    - from: "e2e/global-setup.ts"
      to: "e2e/.auth/user.json"
      via: "storageState save"
      pattern: "storageState.*\\.auth"
    - from: "e2e/*.spec.ts"
      to: "live Vercel deployment"
      via: "baseURL from config"
      pattern: "baseURL"
---

<objective>
Run a security audit using the security-reviewer agent and Supabase MCP advisors (PROC-04), then build a comprehensive Playwright E2E test suite covering all critical application flows against the live deployment.

Purpose: Ensure the application is secure and functionally verified end-to-end before production sign-off.
Output: Security review findings resolved, Playwright config, global auth setup, 6 E2E spec files, passing test suite.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-production/04-RESEARCH.md
@.planning/phases/04-polish-production/04-01-SUMMARY.md
@src/lib/supabase/server.ts
@src/lib/supabase/client.ts
@src/lib/actions/auth.ts
@src/app/api/export/[entity]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security review with security-reviewer agent and Supabase MCP advisors</name>
  <files></files>
  <action>
**Security Review — invoke the security-reviewer agent (PROC-04):**

Per user decision: "Actively leverage available skills, agents, and MCP tools during execution."

1. **Supabase MCP Advisory Check:** Run `get_advisors(type: "security")` and `get_advisors(type: "performance")` on project `ntrliqzjbmhkkqhxtvqe` to get automated findings for RLS policies and database configuration.

2. **Security Reviewer Agent:** Invoke the `security-reviewer` agent (check `.claude/agents/` or available skills) to audit:
   - **Auth endpoints:** `src/lib/supabase/proxy.ts` (session cookie handling), `src/lib/actions/auth.ts` (signIn, signUp, signOut, resetPassword Server Actions)
   - **RLS policies:** All Supabase tables (contacts, organizations, deals, tasks, interactions, deal_contacts, contact_organizations, pipeline_stages, accounts, account_members, profiles)
   - **API routes:** New `/api/export/[entity]/route.ts` from 04-01 — verify auth check before data access
   - **Data handling:** Server Actions all perform `supabase.auth.getUser()` before mutations, CSV export filtered by account via RLS, input sanitization via Zod schemas
   - **Proxy configuration:** `src/proxy.ts` auth guard coverage

3. **Remediation:** Fix any critical or high-severity findings immediately. Document medium/low findings as known issues if not fixable within this task's scope.

4. **Document results** in the plan summary: findings count by severity, what was fixed, what was accepted.

Note: If the security-reviewer agent is not available as a standalone agent, perform the review manually by systematically checking each category above. The Supabase MCP advisors are always available.
  </action>
  <verify>
    <automated>MISSING — security review is agent/MCP-driven, not test-driven. Verify by running: node -e "console.log('Security review task completed')"</automated>
    <manual>Confirm security review document exists with no unresolved critical findings. Check Supabase MCP advisor results show no new security warnings.</manual>
  </verify>
  <done>Security review completed with no critical findings unresolved. Supabase MCP advisors show clean security posture. Any fixes for critical/high findings are applied and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E test suite setup and spec files</name>
  <files>
    playwright.config.ts
    e2e/global-setup.ts
    e2e/contacts.spec.ts
    e2e/organizations.spec.ts
    e2e/deals.spec.ts
    e2e/tasks.spec.ts
    e2e/interactions.spec.ts
    e2e/dashboard.spec.ts
    .gitignore
  </files>
  <action>
**Playwright Configuration (`playwright.config.ts`):**
1. `testDir: './e2e'`
2. `fullyParallel: false` — avoid race conditions with shared live DB.
3. `workers: 1` — serial execution for live DB integrity (per user decision: tests against live Supabase).
4. `retries: 1`
5. `reporter: 'html'`
6. `globalSetup: require.resolve('./e2e/global-setup.ts')`
7. `use.baseURL: process.env.PLAYWRIGHT_BASE_URL || 'https://healthcrm-tawny.vercel.app'`
8. `use.storageState: 'e2e/.auth/user.json'`
9. `use.trace: 'on-first-retry'`
10. `use.screenshot: 'only-on-failure'`
11. Single project: chromium Desktop Chrome.

**Global Setup (`e2e/global-setup.ts`):**
1. Launch chromium browser.
2. Navigate to `${baseURL}/login`.
3. Fill email field with `process.env.TEST_EMAIL`.
4. Fill password field with `process.env.TEST_PASSWORD`.
5. Click sign-in button.
6. Wait for redirect to `/dashboard`.
7. Save `storageState` to `e2e/.auth/user.json`.
8. Close browser.

**Update `.gitignore`:** Add `e2e/.auth/` to prevent committing auth state. Add `test-results/` and `playwright-report/` for Playwright output.

**Test Data Strategy (per user decision: create & cleanup):**
- Each test creates its own data with recognizable prefix `[E2E]` in names/titles.
- `test.afterEach` or `test.afterAll` cleans up created records by navigating to edit/delete flows.
- Tests are isolated and repeatable.

**E2E Spec Files:**

**`e2e/contacts.spec.ts`** — Contacts CRUD:
- Create a contact (`[E2E] Test Contact`) via the contacts page create form
- Verify the contact appears in the contacts list
- Edit the contact (change title or phone)
- Verify the edit is reflected
- Delete the contact via the detail page
- Verify the contact no longer appears in the list

**`e2e/organizations.spec.ts`** — Organizations CRUD:
- Create an organization (`[E2E] Test Org`) via the organizations page
- Verify it appears in the list
- Edit the organization
- Delete the organization
- Verify removal

**`e2e/deals.spec.ts`** — Deals CRUD + Kanban DnD:
- Create a deal (`[E2E] Test Deal`) via the deals page
- Verify it appears on the Kanban board in the correct stage column
- Drag the deal card to a different stage column (add `data-testid` attributes to DealCard and KanbanColumn if not present)
- Verify the deal moved to the new stage
- Delete the deal
- NOTE on DnD: If Playwright `dragTo` does not work with @dnd-kit (per RESEARCH.md pitfall 6), fall back to keyboard-based drag test or skip DnD test with `test.skip` and a comment explaining the limitation.

**`e2e/tasks.spec.ts`** — Tasks CRUD:
- Create a task (`[E2E] Test Task`) via the tasks page
- Verify it appears in the task list
- Complete the task (toggle completion)
- Edit the task
- Delete the task

**`e2e/interactions.spec.ts`** — Interactions CRUD:
- Navigate to a contact detail page
- Log an interaction (`[E2E] Test Interaction`) via the interaction form modal
- Verify it appears in the timeline
- Edit the interaction
- Delete the interaction

**`e2e/dashboard.spec.ts`** — Dashboard Metrics:
- Navigate to /dashboard
- Verify metric cards are visible (pipeline value, deal count, tasks due today, overdue tasks)
- Verify pipeline summary section is visible
- Verify activity feed section is visible
- No specific value assertions (live data changes) — just verify UI elements render without error

**Important:** Use semantic locators (`getByRole`, `getByLabel`, `getByText`) per Playwright best practices. Only use `data-testid` for dnd-kit Kanban elements where semantic locators are insufficient.
  </action>
  <verify>
    <automated>cd "D:/claude code/CRM" && npx playwright test --list 2>&1 | head -30</automated>
    <manual>Run `npx playwright test` — all tests should pass against the live deployment (requires TEST_EMAIL and TEST_PASSWORD env vars)</manual>
  </verify>
  <done>Playwright config exists with correct settings. Global setup authenticates and saves state. All 6 spec files exist with create-and-cleanup test data strategy. `npx playwright test --list` shows all test cases. Tests run against live deployment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify E2E test suite runs against live deployment</name>
  <files>e2e/.auth/user.json</files>
  <action>
Human verification checkpoint. E2E test suite has been built with Playwright targeting the live Vercel+Supabase deployment. Tests cover CRUD for all 5 CRM entities, Kanban drag-and-drop, and dashboard metrics verification.

Steps for the user:
1. Create `.env.test.local` with: `TEST_EMAIL=your-test-user@email.com`, `TEST_PASSWORD=your-password`, `PLAYWRIGHT_BASE_URL=https://healthcrm-tawny.vercel.app` (or your Vercel URL).
2. Run `npx playwright test` from the project root.
3. Expected: All tests pass (or known DnD limitation is documented as skipped).
4. Check `playwright-report/` for the HTML report if needed.
5. Confirm no orphaned `[E2E]` test data remains in the application after tests complete.

Resume signal: Type "tests passing" to continue, or describe failures to fix.
  </action>
  <verify>
    <automated>cd "D:/claude code/CRM" && npx playwright test --list 2>&1 | head -20</automated>
    <manual>User runs full test suite and confirms all tests pass</manual>
  </verify>
  <done>User has confirmed E2E tests pass against the live deployment. No orphaned test data remains. Test coverage includes all 5 entity CRUD flows, Kanban DnD, and dashboard metrics.</done>
</task>

</tasks>

<verification>
1. Security review completed with no critical/high unresolved findings
2. Supabase MCP security advisors checked
3. `playwright.config.ts` exists with correct settings
4. `e2e/global-setup.ts` authenticates and saves storageState
5. All 6 spec files exist and list correctly via `npx playwright test --list`
6. E2E tests pass against live deployment (verified by user)
7. `e2e/.auth/` is gitignored
</verification>

<success_criteria>
- Security review documented with no critical findings unresolved
- Playwright E2E test suite covers all 5 entity CRUD flows, Kanban DnD, and dashboard metrics
- Tests run against live Vercel+Supabase deployment
- User has confirmed tests pass
- No orphaned test data in production
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-production/04-02-SUMMARY.md`
</output>
